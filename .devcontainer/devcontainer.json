{
  "name": "Abi-Verkauf (Django)",
  "image": "mcr.microsoft.com/devcontainers/python:0-3.11",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "forwardPorts": [8000],
  "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && .venv/bin/pip install -r requirements.txt && .venv/bin/python manage.py migrate || true && .venv/bin/python manage.py createdemo || true",
  "remoteUser": "vscode"
}
